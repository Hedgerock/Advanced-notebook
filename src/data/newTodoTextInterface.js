export const newTodoTextInterface = ({ id, status, text, order, deleted, notation }) => {

    return { 
        id, 
        status, 
        text, 
        order, 
        deleted, 
        notation
    }
}